services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage-media
    env_file:
      - .env
    environment:
      HOMEPAGE_ALLOWED_HOSTS: ${HOST}:30000,homepage.local,localhost:3000 # required, may need port. See gethomepage.dev/installation/#homepage_allowed_hosts
    ports:
      - 3001:3000
    volumes:
      - ./:/app/config
      #- /var/run/docker.sock:/var/run/docker.sock:ro # optional, for docker integrations
    restart: unless-stopped
networks: {}
